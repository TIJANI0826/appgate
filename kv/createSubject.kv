<CreateSubject>:
    createSub: createSub
    sub : sub
    TextInput:
        id : sub
        text: "Subject"
    Button:
        id : createSub
        text : "Create New Subject"
        on_release : root.createSubject()